# 🔧 راهنمای تنظیمات پروژه توانا پروکسی

## 🔗 لینک‌های قابل تغییر

### فایل اصلی: `src/config/links.ts`

این فایل شامل تمامی لینک‌های خارجی و قابل تغییر پروژه است.

| متغیر | مقدار فعلی | توضیح |
|-------|-----------|-------|
| `PANEL_URL` | `"#"` | آدرس پنل کاربری وردپرس |
| `PANEL_LOGIN` | `"#/login"` | صفحه ورود پنل |
| `PANEL_REGISTER` | `"#/register"` | صفحه ثبت‌نام پنل |
| `PANEL_DASHBOARD` | `"#/dashboard"` | داشبورد پنل |
| `BLOG_URL` | `"#"` | آدرس بلاگ |
| `TELEGRAM_CHANNEL` | `"https://t.me/panbehnet"` | کانال تلگرام |
| `TELEGRAM_SUPPORT` | `"https://t.me/Rahbarusd"` | پشتیبانی تلگرام |
| `SUPPORT_PHONE` | `"+989901120235"` | شماره پشتیبانی |
| `SUPPORT_EMAIL` | `"support@tawana.proxy"` | ایمیل پشتیبانی |

### لینک‌های دانلود:

| متغیر | توضیح |
|-------|-------|
| `DOWNLOAD_ANDROID` | فایل APK اندروید |
| `DOWNLOAD_IOS` | لینک App Store |
| `DOWNLOAD_WINDOWS` | نصاب ویندوز |
| `DOWNLOAD_MAC` | نصاب macOS |
| `DOWNLOAD_LINUX` | نصاب لینوکس |
| `DOWNLOAD_CHROME` | افزونه کروم |

---

## 📋 نحوه جایگزینی لینک‌ها

### مرحله ۱: ویرایش فایل

```bash
cd src/config
nano links.ts  # یا ویرایشگر دلخواه خود
```

### مرحله ۲: به‌روزرسانی مقادیر

```typescript
export const LINKS = {
  // مثال: جایگزینی لینک پنل
  PANEL_URL: "https://panel.tawanaproxy.com",
  PANEL_LOGIN: "https://panel.tawanaproxy.com/login",
  
  // مثال: جایگزینی لینک بلاگ
  BLOG_URL: "https://blog.tawanaproxy.com",
  
  // سایر لینک‌ها...
};
```

### مرحله ۳: بیلد پروژه

```bash
npm run build
```

### مرحله ۴: Deploy

مطابق با روش deploy خود (GitHub Pages، Netlify، Vercel و...)

---

## 🌐 تنظیمات دامنه اختصاصی (GitHub Pages)

### مرحله ۱: افزودن دامنه

1. به **Settings** → **Pages** بروید
2. در بخش **Custom domain** دامنه خود را وارد کنید
3. روی **Save** کلیک کنید

### مرحله ۲: تنظیم DNS

در پنل مدیریت دامنه خود، رکوردهای زیر را اضافه کنید:

#### برای Apex Domain (مثال: `tawanaproxy.com`):

```
A Record:
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

#### برای Subdomain (مثال: `www.tawanaproxy.com`):

```
CNAME Record:
TawanaProxyOfficial.github.io
```

### مرحله ۳: فعال‌سازی HTTPS

1. منتظر بمانید تا DNS propagate شود (تا ۲۴ ساعت)
2. در GitHub Pages، گزینه **Enforce HTTPS** را فعال کنید

---

## 🎨 تنظیمات تم و استایل

### رنگ‌های اصلی (تم تاریک):

فایل: `src/styles/themes/dark.css`

```css
--accent-primary: #00D4AA;    /* سبز نئون */
--accent-purple: #7C3AED;     /* بنفش */
--bg-primary: #0a0a0a;        /* پس‌زمینه */
```

### رنگ‌های اصلی (تم روشن):

فایل: `src/styles/themes/light.css`

```css
--accent-primary: #00B894;    /* سبز تیره */
--accent-secondary: #6C5CE7;  /* بنفش */
--text-primary: #1a1a2e;      /* متن */
```

---

## 🚀 دستورات مهم

### Development (توسعه محلی):
```bash
npm run dev
```

### Build (ساخت نسخه نهایی):
```bash
npm run build
```

### Preview (پیش‌نمایش نسخه build شده):
```bash
npm run preview
```

### Lint (بررسی کد):
```bash
npm run lint
```

---

## 📄 صفحات موجود

| مسیر | توضیح | وضعیت |
|------|-------|-------|
| `/` | صفحه اصلی | ✅ |
| `/about` | درباره ما | ✅ |
| `/contact` | تماس با ما | ✅ |
| `/download` | دانلود | ✅ |
| `/features` | ویژگی‌ها | ✅ |
| `/pricing` | قیمت‌ها | ✅ |
| `/faq` | سوالات متداول | ✅ |
| `/terms` | قوانین و مقررات | ✅ جدید |
| `/privacy` | حریم خصوصی | ✅ جدید |
| `/refund` | سیاست استرداد | ✅ جدید |

---

## 🔐 امنیت

### بررسی آسیب‌پذیری‌ها:

```bash
npm audit
```

### رفع آسیب‌پذیری‌های خودکار:

```bash
npm audit fix
```

---

## 📞 پشتیبانی

در صورت بروز مشکل یا سوال:

- **تلگرام پشتیبانی:** [@Rahbarusd](https://t.me/Rahbarusd)
- **کانال تلگرام:** [@panbehnet](https://t.me/panbehnet)
- **ایمیل:** support@tawana.proxy

---

## 📝 یادداشت‌های مهم

1. **بعد از هر تغییر در config، حتماً `npm run build` بزنید**
2. **قبل از deploy، با `npm run preview` تست کنید**
3. **از git برای مدیریت نسخه‌ها استفاده کنید**
4. **فایل‌های `.env` را commit نکنید (اگر دارید)**
5. **برای بهینه‌سازی عکس‌ها از ابزارهای فشرده‌سازی استفاده کنید**

---

**آخرین بروزرسانی:** ۱۵ آذر ۱۴۰۳  
**نسخه:** 2.0.0
